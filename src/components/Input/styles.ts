import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textInput: {
    height: 40,
    width: '75%',
    borderWidth: 1,
    borderRadius: 10,
    padding: 10,
  },
  textInputError: {
    borderColor: 'red',
  },
});
